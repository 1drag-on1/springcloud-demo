spring:
  application:
    name: merchantservice
  profiles:
    active: dev  #环境
  cloud:
    nacos:
      server-addr: 47.120.15.196:8848  #nacos地址
      discovery:
#        cluster-name: ZZ #集群名称
        cluster-name: ZS #集群名称
      config:
        file-extension: yaml  #文件后缀名